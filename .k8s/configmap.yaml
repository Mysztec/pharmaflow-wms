apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: pharmaflow
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  NODE_ENV: "production"
  API_URL: "http://sistema.seudominio.com/api"